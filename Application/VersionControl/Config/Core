#!/usr/bin/env bash

# Do not edit this file unless you're sure what your doing

# genesis directory
export genesis_dir=$(dirname "${BASH_SOURCE[0]}")/../../..;

# version control dir
export genesis_vc=$(dirname "${BASH_SOURCE[0]}")/..;

# VersionUpdate file
export genesis_vu=$(dirname "${BASH_SOURCE[0]}")/../UpdateVersion;